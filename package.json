{
  "name": "@zyto/zapier-connector",
  "version": "0.1.0",
  "type": "module",
  "description": "Zapier invocation wrapper with retries and telemetry.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint . --ext .ts",
    "test": "vitest run",
    "prepare": "husky install"
  },
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "axios": "^1.7.7",
    "p-retry": "^6.2.0",
    "zod": "^3.23.8"
  },
